import React from "react";
import { Route } from "react-router-dom";
import { Typography } from "@material-ui/core";

const CreateQuizPage=(props)=>{
    return (
        <Typography component="h1" variant="h1">No make create quiz page lollll</Typography>
    )
}
const QuizCreatorPage=(props)=>{
    return (
        //<Route path='/' component={CreateQuizPage} />
        <Typography component="h1" variant="h1">LOL</Typography>
    ) 
}

export default QuizCreatorPage;
